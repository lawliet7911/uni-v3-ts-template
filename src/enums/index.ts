export enum HttpStatus {
  NOT_FOUND = 404,
  SUCCESS = 200,
  ERROR = 500
}

export enum HTTPMethod {
  OPTIONS = 'OPTIONS',
  GET = 'GET',
  HEAD = 'HEAD',
  POST = 'POST',
  PUT = 'PUT',
  DELETE = 'DELETE',
  TRACE = 'TRACE',
  CONNECT = 'CONNECT'
}
